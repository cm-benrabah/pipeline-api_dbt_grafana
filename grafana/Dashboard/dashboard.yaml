apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10  # Interval to check for changes (in seconds)
    options:
      path: /etc/grafana/provisioning/dashboards  # Path to your JSON files
